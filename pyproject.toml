[project]
name = "basic-akgents"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Jettro Coenradie", email = "jettro@coenradie.com" }
]
requires-python = ">=3.12"
dependencies = [
    "akgentic-core>=1.5.4",
]

[project.scripts]
basic-akgents = "basic_akgents:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
